import { ImagedirectiveDirective } from './imagedirective.directive';

describe('ImagedirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagedirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
